let img; //Declare varriable 'img'.

function preload() {
  img = loadImage('Antenna.jpg'); //Load image
}

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}
